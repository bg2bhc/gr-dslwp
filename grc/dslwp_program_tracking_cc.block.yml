# auto-generated by grc.converter

id: dslwp_program_tracking_cc
label: Program Tracking CC
category: '[DSLWP]'

parameters:
-   id: enable
    label: Enable
    dtype: enum
    default: '1'
    options: ['0', '1', '2']
    option_labels: ['Off', 'On', Replay]
-   id: timestamp
    label: Timestamp
    dtype: raw
    default: '1526774400'
-   id: path
    label: Path
    dtype: file_open
-   id: lon
    label: Longtitude
    dtype: float
-   id: lat
    label: Latitude
    dtype: float
-   id: alt
    label: Altitude (m)
    dtype: float
-   id: fc
    label: Center Frequency (Hz)
    dtype: float
-   id: samp_rate
    label: Sample Rate (Hz)
    dtype: raw
-   id: txrx
    label: TXRX
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [TX, RX]
-   id: verbose
    label: Verbose
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: ['On', 'Off']

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import dslwp
    make: dslwp.program_tracking_cc(${enable}, ${timestamp}, ${path}, ${lon}, ${lat},
        ${alt}, ${fc}, ${samp_rate}, ${txrx}, ${verbose})

file_format: 1
