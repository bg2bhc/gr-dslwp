# auto-generated by grc.converter

id: dslwp_ccsds_pseudo_randomizer
label: CCSDS Pseudo Randomizer
category: '[DSLWP]'

parameters:
-   id: data_format
    label: Data Format
    dtype: enum
    default: '1'
    options: ['1', '2', '3']
    option_labels: [Bits, Packed bits, Float Bits]

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import dslwp
    make: dslwp.ccsds_pseudo_randomizer(${data_format})

file_format: 1
