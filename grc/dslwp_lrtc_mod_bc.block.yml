id: dslwp_lrtc_mod_bc
label: LRTC Mod BC
category: '[DSLWP]'

templates:
  imports: from gnuradio import dslwp
  make: dslwp.lrtc_mod_bc()


inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex


#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
