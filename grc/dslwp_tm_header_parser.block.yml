id: dslwp_tm_header_parser
label: TM Header Parser
category: '[DSLWP]'

templates:
  imports: from gnuradio import dslwp
  make: dslwp.tm_header_parser()


inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out 0
    optional: true
-   domain: message
    id: out 1
    optional: true
-   domain: message
    id: out 2
    optional: true
-   domain: message
    id: out 3
    optional: true

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
