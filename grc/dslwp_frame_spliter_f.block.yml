# auto-generated by grc.converter

id: dslwp_frame_spliter_f
label: Frame Spliter F
category: '[DSLWP]'

parameters:
-   id: key
    label: Key
    dtype: string
    default: payload_start
-   id: frame_length
    label: Frame Length
    dtype: int
    default: '3576'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: message
    id: out

templates:
    imports: import dslwp
    make: dslwp.frame_spliter_f(${key}, ${frame_length})

file_format: 1
