# auto-generated by grc.converter

id: dslwp_set_gain_by_tag_cc
label: Set Gain by Tag CC
category: '[DSLWP]'

parameters:
-   id: k
    label: K
    dtype: float
    default: '512'
-   id: gain
    label: Gain
    dtype: float
    default: '1'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import dslwp
    make: dslwp.set_gain_by_tag_cc(${k}, ${gain})

file_format: 1
