# auto-generated by grc.converter

id: dslwp_differential_phase_detection_1bit_cf
label: Differential Phase Detection 1bit CF
category: '[DSLWP]'

parameters:
-   id: samples_per_symbol
    label: Samples per Symbol
    dtype: int
-   id: taps
    label: Taps
    dtype: complex_vector
-   id: opt_point
    label: Opt. Point
    dtype: int
-   id: delay
    label: Delay
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import dslwp
    make: dslwp.differential_phase_detection_1bit_cf(${samples_per_symbol}, ${taps},
        ${opt_point}, ${delay})

file_format: 1
