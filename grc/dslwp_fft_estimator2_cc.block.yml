id: dslwp_fft_estimator2_cc
label: FFT Estimator2 CC
category: '[DSLWP]'

templates:
  imports: from gnuradio import dslwp
  make: dslwp.fft_estimator2_cc(${fft_size}, ${threshold}, ${tap_len})

parameters:
-   id: fft_size
    label: FFT Size
    dtype: int
    hide: ${ 'part' if fft_size == 1 else 'none' }
-   id: threshold
    label: Threshold
    dtype: float
-   id: tap_len
    label: Tap Length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ fft_size }
    multiplicity: '2'

outputs:
-   label: out
    domain: stream
    dtype: complex
-   label: pwr
    domain: stream
    dtype: float
    optional: true
-   label: freq
    domain: stream
    dtype: float
    optional: true
-   label: snr
    domain: stream
    dtype: float
    optional: true

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
